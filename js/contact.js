function validate_form() {	var name = document.forms['contact_form']['name'].value;	var address = document.forms['contact_form']['address'].value;	var content = document.getElementById('textarea').value;	var auth_code = document.forms['contact_form']['auth_code'].value;		if (name == null || name== '') {		alert('Bạn chưa điền tên của mình !');		return false;	}		if (address == null || address== '') {		alert('Bạn chưa điền địa chỉ liên hệ của mình !');		return false;	}		if (content == null || content== '') {		alert('Bạn chưa nhận nội dung thư !');		return false;	} else {		if (content.length  < 15) {			alert('Thư của bạn phải dài tối thiểu 15 kí tự !\n\nHiện thư của bạn chỉ mới dài ' + content.length + ' kí tự');			return false;		}	}	if (auth_code == null || auth_code=='' || auth_code.length != 8) {		alert('Mã xác nhận của bạn không hợp lệ !');		return false;	}		return true;}function reloadImg(id) {   var obj = document.getElementById(id);   var src = obj.src;   var pos = src.indexOf('?');   if (pos >= 0) {	  src = src.substr(0, pos);   }   var date = new Date();   obj.src = src + '?v=' + date.getTime();   return false;}